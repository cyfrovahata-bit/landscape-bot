BOT_TOKEN=
GOOGLE_SHEET_ID=
GOOGLE_SERVICE_ACCOUNT_EMAIL=
GOOGLE_PRIVATE_KEY=
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/landscape"
SYNC_INTERVAL_MS=45000
# The mini-app's own public HTTPS URL (same one registered as its Mini App
# in @BotFather) -- lets admin approval notifications include a button that
# opens the report directly. Notifications still send without it if unset.
PUBLIC_APP_URL=
